import { Routes } from '@angular/router';
import { ScanReceivingComponent } from './scan-receiving/scan-receiving.component';

export const RECEIVING_ROUTES: Routes = [
  { path: '', component: ScanReceivingComponent }
];